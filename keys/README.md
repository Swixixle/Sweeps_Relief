# Keys directory

Relief is a signature-verification endpoint, not a signer of external artifacts
(yet). This directory holds Relief's own internal signing keys (policy.json,
log bundles) per existing Relief architecture.

For verifying artifacts FROM Sweeps_Intel, see trust_store.json at the repo
root. That file holds Intel's public keys.
